# 小电脑到电控端协议说明

## 1. 一句话说明

小电脑端通过串口发给电控端的，不是原始图像，也不是官方 `0x0310` 本体，而是：

- `自定义协议头`
- `H.264 Annex-B` 码流切片或配置/心跳数据

电控端只需要：

- 收串口
- 校验包
- 排队
- 原样塞进官方 `0x0310`

## 2. 物理链路

- 传输方式：`UART`
- 串口格式：`8N1`
- 波特率：`1500000`
- 备选波特率：`921600`
- 字节序：`小端`

## 3. 整包结构

每个小电脑端发给电控端的包都长这样：

```text
[ 16字节协议头 ][ payload ]
```

约束：

- 单包最大总长度：`300` 字节
- 单包最大 payload：`284` 字节

## 4. 协议头定义

### 4.1 结构体

```c
typedef struct __attribute__((packed)) {
    uint16_t magic;        // 固定 0x4856
    uint8_t  version;      // 固定 1
    uint8_t  packet_type;  // 1=config, 2=stream_chunk, 3=heartbeat, 4=reset
    uint8_t  stream_id;    // 固定 1
    uint8_t  codec;        // 1=h264_annexb, 2=hevc_annexb, 3=mjpeg
    uint8_t  flags;        // bit0=关键帧提示, bit1=配置重发, bit2=流不连续
    uint8_t  reserved0;    // 固定 0
    uint32_t seq;          // 包序号，每发一个包递增 1
    uint16_t payload_len;  // payload 长度，最大 284
    uint16_t packet_crc16; // CRC16/CCITT-FALSE
} hero_video_packet_header_v1_t;
```

### 4.2 字节级偏移表

| 偏移 | 长度 | 字段名 | 说明 |
|---|---:|---|---|
| `0` | `2` | `magic` | 固定 `0x4856` |
| `2` | `1` | `version` | 固定 `1` |
| `3` | `1` | `packet_type` | 包类型 |
| `4` | `1` | `stream_id` | 当前固定 `1` |
| `5` | `1` | `codec` | 当前建议固定 `1`，即 `H.264 Annex-B` |
| `6` | `1` | `flags` | 标志位 |
| `7` | `1` | `reserved0` | 保留位，填 `0` |
| `8` | `4` | `seq` | 包序号 |
| `12` | `2` | `payload_len` | 有效载荷长度 |
| `14` | `2` | `packet_crc16` | 整包校验 |

## 5. `packet_type` 定义

| 值 | 名称 | 用途 |
|---:|---|---|
| `1` | `config` | 发送视频参数 |
| `2` | `stream_chunk` | 发送视频码流切片 |
| `3` | `heartbeat` | 发送链路状态统计 |
| `4` | `reset` | 通知下游清空缓存，等待新关键帧 |

## 6. `flags` 定义

| 位 | 名称 | 含义 |
|---:|---|---|
| `bit0` | `keyframe_hint` | 当前码流切片来自关键帧 |
| `bit1` | `config_repeat` | 当前配置包是周期性重发 |
| `bit2` | `discontinuity` | 流发生不连续或重置 |

## 7. payload 内容说明

### 7.1 `config` 包

用途：告诉电控端和客户端当前视频流参数。

结构：

```c
typedef struct __attribute__((packed)) {
    uint16_t width;
    uint16_t height;
    uint16_t fps;
    uint16_t target_bytes_per_sec;
} hero_video_config_v1_t;
```

说明：

- 长度固定 `8` 字节
- 启动时先发 `1` 次
- 之后每 `1s` 重发 `1` 次
- 分辨率或码率变更时立刻重发

### 7.2 `stream_chunk` 包

用途：真正承载视频数据。

内容：

- `H.264 Annex-B` 连续码流的一个切片

说明：

- 每个切片最大 `284` 字节
- 一个编码输出可能被切成多个 `stream_chunk`
- 电控端不需要理解视频帧边界，只需要原样转发

### 7.3 `heartbeat` 包

用途：给电控端和联调人员看状态。

结构：

```c
typedef struct __attribute__((packed)) {
    uint32_t captured_frames;
    uint32_t encoded_frames;
    uint32_t sent_packets;
    uint32_t dropped_packets;
} hero_video_heartbeat_v1_t;
```

说明：

- 当前长度固定 `16` 字节
- 建议 `1Hz` 发送

### 7.4 `reset` 包

用途：让下游清空状态并等待新的关键帧。

说明：

- payload 长度为 `0`
- 启动时会先发一次
- 退出或严重异常时也可以发

## 8. CRC 计算规则

- 算法：`CRC16/CCITT-FALSE`
- 初始值：`0xFFFF`
- 多项式：`0x1021`
- 不做反转
- 最终值不异或

计算范围：

- 协议头前 `14` 字节，也就是从 `magic` 到 `payload_len`
- 再加上整个 `payload`
- 不包含 `packet_crc16` 自己这 2 个字节

也就是说：

```text
CRC = CRC16( header[0..13] + payload[0..payload_len-1] )
```

## 9. 小电脑端实际发送内容总结

小电脑端发给电控端的内容，按时间顺序大概是：

1. `reset`
2. `config`
3. 一串连续的 `stream_chunk`
4. 周期性 `heartbeat`
5. 周期性重发 `config`
6. 若关键帧到来，则对应的 `stream_chunk.flags.bit0 = 1`

## 10. 电控端收到后要做什么

### 10.1 电控端最小职责

电控端不负责：

- 解码视频
- 还原图像
- 重编码视频

电控端只负责：

1. 串口收包
2. 按 `magic` 找包头
3. 读取完整 `16 + payload_len` 字节
4. 校验 `CRC16`
5. 把整包放入待发送队列
6. 按 `50Hz` 节奏发到官方 `0x0310`

### 10.2 推荐处理流程

```text
串口 DMA 收字节
-> 扫描 magic=0x4856
-> 收满 16 字节包头
-> 检查 version / payload_len
-> 收满 payload
-> 做 CRC16 校验
-> 合法则入队
-> 20ms 到时取 1 包
-> 原样塞进官方 0x0310
```

## 11. 电控端如何映射到 `0x0310`

最关键的一点：

- 一个小电脑端完整协议包
- 对应一个官方 `0x0310` 的 payload

也就是：

```text
0x0310.data[0..N-1] = 小电脑端整个协议包
```

其中：

- `N = 16 + payload_len`
- 必须满足 `N <= 300`

当前协议就是按这个限制设计的，所以正常不会超。

## 12. 一个例子

### 12.1 配置包例子

假设：

- `width = 300`
- `height = 300`
- `fps = 60`
- `target_bytes_per_sec = 11000`

那么小电脑端会发一个：

- `packet_type = 1`
- `payload_len = 8`
- `payload = [300, 300, 60, 11000]` 的小端二进制

### 12.2 视频切片包例子

假设编码器产出 `900` 字节码流：

- 第 1 包：`payload_len = 284`
- 第 2 包：`payload_len = 284`
- 第 3 包：`payload_len = 284`
- 第 4 包：`payload_len = 48`

这四个包会带连续 `seq` 发给电控端。

## 13. 联调时如何判断电控端收对了

电控端收对时，应该逐步看到：

- 能解析到第一包
- `CRC` 错误很少或没有
- 能发出第一包 `0x0310`
- 队列不会持续爆满

如果有问题，优先看：

- 串口波特率
- 地线是否共地
- `DMA` 写指针是否正确
- `0x0310` 发送函数是否真的发成功

## 14. 给电控同学的一句话版本

你可以直接把这句话给电控同学：

- 小电脑端每次通过串口发一个不超过 `300` 字节的自定义包，包头固定 `16` 字节，小端，带 `CRC16`；视频数据是 `H.264 Annex-B` 切片；电控端收到后校验并原样塞进官方 `0x0310` 即可，不需要解码视频。
