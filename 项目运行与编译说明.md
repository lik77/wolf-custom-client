# 项目运行与编译说明

## 1. 目录说明

当前目录包含 3 个独立工程：

- `hero-camera-agent`：英雄车小电脑端
- `rm-video-bridge`：`MCU` 电控端桥接模块
- `rm-custom-client`：Ubuntu 自定义客户端

另外还有：

- `三端接口定义文档.md`：三端接口和链路设计说明

## 2. 总体运行流程

整套链路按下面顺序工作：

1. `hero-camera-agent` 在英雄车小电脑上采集海康 `MV-CS016` 相机画面
2. 小电脑对画面预处理、编码、切片，并通过串口发送给 `MCU`
3. `rm-video-bridge` 在 `MCU` 上接收这些小包，并通过官方 `0x0310` 上行链路发给自定义客户端
4. `rm-custom-client` 在 Ubuntu 上通过 `MQTT + Protobuf` 订阅 `CustomByteBlock`，恢复英雄车自研视频画面
5. `rm-custom-client` 同时监听 `UDP 3334`，解码显示官方图传 `HEVC` 画面

## 3. 环境准备

### 3.1 小电脑端 `hero-camera-agent`

建议环境：

- `Ubuntu 20.04/22.04 x86_64`
- 海康 `MVS SDK`
- `OpenCV 4`
- `FFmpeg`

安装系统依赖：

```bash
sudo apt update
sudo apt install -y build-essential cmake pkg-config libopencv-dev \
  libavcodec-dev libavutil-dev libswscale-dev
```

安装海康 `MVS SDK` 后，确认这些路径至少有一套存在：

- `/opt/MVS/include/MvCameraControl.h`
- `/opt/MVS/lib/64/libMvCameraControl.so`

如果 `MVS SDK` 没装在 `/opt/MVS`，先执行：

```bash
export MVS_SDK_ROOT=/你的/MVS/安装目录
```

### 3.2 Ubuntu 客户端 `rm-custom-client`

建议环境：

- `Ubuntu 20.04/22.04 x86_64`
- `Qt5` 或 `Qt6`
- `protobuf`
- `FFmpeg`
- `Paho MQTT C/C++`

安装系统依赖：

```bash
sudo apt update
sudo apt install -y build-essential cmake pkg-config protobuf-compiler libprotobuf-dev \
  qtbase5-dev libqt5network5 libavcodec-dev libavutil-dev libswscale-dev \
  libpaho-mqtt-dev libpaho-mqttpp-dev
```

如果你打算用 `Qt6`，可以改成：

```bash
sudo apt install -y qt6-base-dev
```

说明：

- 当前机器从库名看已经有 `Qt5` 运行库，推荐先按 `Qt5` 编译
- `Paho` 对应的开发包装名是：
- `libpaho-mqtt-dev`
- `libpaho-mqttpp-dev`

### 3.3 `MCU` 电控端 `rm-video-bridge`

这部分不是在 Ubuntu 上运行，而是把源码集成进你的 `MCU` 工程。

需要你现有工程具备：

- 串口 `DMA` 接收能力
- 一个 `50Hz` 节拍
- 官方 `0x0310` 发送接口

## 4. 编译方法

### 4.1 编译 `hero-camera-agent`

进入目录：

```bash
cd hero-camera-agent
```

生成并编译：

```bash
cmake -S . -B build
cmake --build build -j
```

当前状态：

- 该工程已经在本机编译通过

### 4.2 编译 `rm-video-bridge`

这个目录我补了一个本地 `CMake`，用于先验证通用桥接逻辑是否能编译通过。

进入目录：

```bash
cd rm-video-bridge
```

生成并编译：

```bash
cmake -S . -B build
cmake --build build -j
```

当前状态：

- 该工程已经在本机编译通过
- 真正上板时，你需要把源码并入你的 `MCU` 工程，而不是直接运行 Ubuntu 下的 demo

### 4.3 编译 `rm-custom-client`

进入目录：

```bash
cd rm-custom-client
```

生成并编译：

```bash
cmake -S . -B build
cmake --build build -j
```

当前状态：

- 当前机器编译卡在 `Paho MQTT` 开发库未安装
- 需要先安装：
- `libpaho-mqtt-dev`
- `libpaho-mqttpp-dev`

如果你装完仍然报找不到库，可以检查下面文件是否存在：

- `/usr/lib/x86_64-linux-gnu/libpaho-mqtt3as.so`
- `/usr/lib/x86_64-linux-gnu/libpaho-mqttpp3.so`

## 5. 运行方法

### 5.1 运行 `hero-camera-agent`

推荐方式：

```bash
./build/hero-camera-agent --config config/hero-camera-agent.yaml
```

其中大部分常用参数都已经放进 `hero-camera-agent/config/hero-camera-agent.yaml`，调参时直接改这个文件即可。

真实相机模式：

```bash
./build/hero-camera-agent --serial /dev/ttyUSB0 --baud 1500000 --source mvs --device-index 0 --fps 60 --width 300 --height 300 --target-bytes 11000 --exposure-us 12000
```

如果小电脑同时接了两台相同型号海康相机，请先到 `hero-camera-agent/config/hero-camera-agent.yaml` 填写 `camera.device_serial`；改完后，启动命令本身不用变化。

视频文件联调模式：

```bash
./build/hero-camera-agent --serial /dev/ttyUSB0 --baud 1500000 --video /path/to/test.mp4 --fps 60 --width 300 --height 300 --target-bytes 11000
```

### 5.2 运行 `rm-custom-client`

确保网络配置如下：

- 官方选手端主机连接自定义客户端的网卡：`192.168.12.1/24`
- Ubuntu 自定义客户端：`192.168.12.2/24`

启动命令：

```bash
./build/rm-custom-client --mqtt-host 192.168.12.1 --mqtt-port 3333 --udp-port 3334 --robot-id 1
```

参数说明：

- `--mqtt-host`：官方选手端主机地址
- `--mqtt-port`：固定 `3333`
- `--udp-port`：固定 `3334`
- `--robot-id`：客户端 `clientID`，红方英雄填 `1`，蓝方英雄填 `11`

### 5.3 `rm-video-bridge` 上板接入

把下面文件并入你的 `MCU` 工程：

- `rm-video-bridge/Inc/hero_protocol.h`
- `rm-video-bridge/Inc/crc16.h`
- `rm-video-bridge/Inc/bridge_platform.h`
- `rm-video-bridge/Inc/bridge.h`
- `rm-video-bridge/Src/crc16.c`
- `rm-video-bridge/Src/bridge.c`

然后实现 `bridge_platform.h` 中的 4 个函数：

- `bridge_platform_uart_start_rx_dma()`
- `bridge_platform_uart_rx_write_index()`
- `bridge_platform_tick_ms()`
- `bridge_platform_send_custom_0310()`

## 6. 联调顺序建议

建议按这个顺序联调：

1. 先只启动官方选手端，确认官方图传画面正常
2. 再启动 `rm-custom-client`，确认左侧官方 `UDP 3334` 画面正常显示
3. 然后让 `hero-camera-agent` 用视频文件模式跑起来，验证 `MCU` 串口和 `0x0310` 链路
4. 最后接入真实海康相机

## 7. 当前编译验证状态

我已经在当前机器上验证到以下结果：

- `hero-camera-agent`：已编译通过
- `rm-video-bridge`：已编译通过
- `rm-custom-client`：因缺少 `Paho MQTT` 开发库，尚未完成编译

## 8. 常见问题

### 8.1 客户端编译报找不到 `paho-mqttpp3`

先安装：

```bash
sudo apt install -y libpaho-mqtt-dev libpaho-mqttpp-dev
```

### 8.2 小电脑端找不到 `MvCameraControl.h`

确认：

- `/opt/MVS/include/MvCameraControl.h`
- 或设置了正确的 `MVS_SDK_ROOT`

### 8.3 运行客户端看不到 `UDP 3334` 画面

确认：

- 官方选手端已经正常显示图传画面
- Ubuntu 机器 IP 为 `192.168.12.2`
- 没有防火墙拦截 `3334/udp`

### 8.4 右侧英雄车画面不出

优先排查：

- `MCU` 是否真的把整包透传进 `0x0310`
- `CustomByteBlock` 是否持续到达
- 是否先收到 `config` 和关键帧
