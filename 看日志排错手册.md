# 看日志排错手册

## 1. 目的

这份手册用于在三端联调时，快速判断问题卡在：

- 小电脑采集
- 小电脑编码
- 小电脑到 `MCU` 串口传输
- `MCU` 桥接解析
- `MCU` 到官方 `0x0310`
- 官方选手端到自定义客户端 `MQTT`
- 官方 `UDP 3334`
- Ubuntu 客户端解码显示

## 2. 三端日志位置

### 2.1 小电脑端 `hero-camera-agent`

- 终端直接输出中文日志
- 关键代码：`hero-camera-agent/src/main.cpp`

日志前缀：

- `[信息]`
- `[警告]`
- `[错误]`

### 2.2 电控端 `rm-video-bridge`

- 通过 `bridge_platform_debug_log(const char* message)` 输出
- 你需要把它接到自己的 `MCU` 调试串口、RTT 或日志系统
- 关键代码：`rm-video-bridge/Src/bridge.c`

### 2.3 Ubuntu 客户端 `rm-custom-client`

- 日志显示在界面底部日志框
- 关键代码：
- `rm-custom-client/src/mqtt_client.cpp`
- `rm-custom-client/src/custom_stream_handler.cpp`
- `rm-custom-client/src/official_udp_receiver.cpp`
- `rm-custom-client/src/video_decoder.cpp`

## 3. 正常联调时应该看到什么

### 3.1 小电脑端正常日志

启动后通常应该依次看到：

- `[信息] hero-camera-agent 启动中`
- `[信息] 相机源初始化完成`
- `[信息] 串口打开成功，准备向 MCU 发送数据`
- `[信息] 图像预处理器和编码器初始化完成`
- `[警告] 已发送重置包，通知下游清空缓存并等待新关键帧`
- `[信息] 已发送首个配置包...`
- `[信息] 已成功读取第一帧原始画面...`
- `[信息] 已编码出第一段视频码流...`

之后每隔一段时间会看到：

- 关键帧日志
- 心跳包日志
- 运行统计日志

### 3.2 `MCU` 正常日志

上电并启动桥接后，通常应该依次看到：

- `[信息] rm-video-bridge 初始化完成，已启动串口 DMA 接收`
- `[信息] 已成功解析到第一包来自小电脑的视频数据`
- `[信息] 已成功发送第一包 0x0310 自定义数据`

### 3.3 Ubuntu 客户端正常日志

客户端启动后通常应该看到：

- `[信息] 正在连接 MQTT 服务器...`
- `[信息] MQTT 连接成功，开始订阅官方 Topic`
- `[信息] MQTT Topic 订阅完成...`
- `[信息] 已开始监听 UDP 3334，等待官方图传 HEVC 码流`

如果官方图传正常：

- `[信息] 已收到官方图传 UDP 包...`
- `[信息] 已完成一帧官方 HEVC 码流重组...`
- `[信息] 已成功解码图像帧...`

如果自研链路正常：

- `[信息] 已收到 CustomByteBlock...`
- `[信息] 已解析自研视频包...`
- `[信息] 已收到自研视频配置包...`
- `[信息] 已收到新的关键帧，自研视频开始恢复解码`
- `[信息] 已成功解码图像帧...`

## 4. 按故障现象排查

### 4.1 小电脑端一启动就退出

看是否出现：

- `[错误] hero-camera-agent 发生异常: ...`

常见原因：

- 串口设备名错，例如 `/dev/ttyUSB0` 不存在
- `MVS SDK` 未正确安装
- 相机没连上
- `--source` 参数不对

优先检查：

- `ls /dev/ttyUSB*`
- `echo $MVS_SDK_ROOT`
- 海康相机在 `MVS` 工具里是否能看到

### 4.2 小电脑端能启动，但一直没有“已成功读取第一帧原始画面”

如果反复出现：

- `[警告] 读取相机画面失败...`

说明问题在“相机采集”这一步。

常见原因：

- 相机没被系统识别
- 设备序号不对
- 曝光、帧率设置异常
- USB 带宽或供电有问题

建议检查：

- 换短一点、质量更好的 USB 线
- 降低相机帧率试一下
- 先用 `MVS` 官方工具确认出图

### 4.3 小电脑端能读到帧，但一直没有“已编码出第一段视频码流”

说明问题在“编码器输出”这一步。

检查：

- 编码器依赖是否正常
- 输出分辨率是否异常
- `target-bytes` 是否设得太低

建议先用：

- `--width 300 --height 300`
- `--target-bytes 11000`

### 4.4 小电脑端正常发送，但 `MCU` 一直没有“已成功解析到第一包来自小电脑的视频数据”

说明问题在“小电脑到 `MCU` 串口链路”。

优先检查：

- 波特率是否一致，建议先固定 `1500000`
- 串口线是否接反
- 地线是否共地
- `DMA` 是否真的启动了
- `bridge_platform_uart_rx_write_index()` 返回值是否正确

### 4.5 `MCU` 日志出现“串口包 CRC 校验失败”

如果看到：

- `[错误] 串口包 CRC 校验失败，请检查波特率、地线或 DMA 写指针`

重点怀疑：

- 波特率不稳
- 地线问题
- 串口硬件质量问题
- `DMA` 写指针计算错
- 收到的数据被别的日志串口干扰

建议处理顺序：

1. 先确认不是复用调试串口
2. 再把波特率降到 `921600`
3. 再检查 `DMA NDTR` 到写指针的换算

### 4.6 `MCU` 能解析第一包，但发不出 `0x0310`

如果看到：

- `[错误] 发送 0x0310 失败，请检查裁判系统发送接口或链路状态`

说明问题在 `bridge_platform_send_custom_0310()`。

重点检查：

- 官方裁判系统发送接口是否真的成功返回
- 图传链路是否在线
- 机器人与发送端通信是否正常
- 你的 `0x0310` 封装函数是否接受完整 300 字节以内 payload

### 4.7 Ubuntu 客户端 MQTT 连接不上

如果看到：

- `[错误] MQTT 连接失败: ...`

优先检查：

- 官方选手端主机连接自定义客户端的网卡是不是 `192.168.12.1`
- Ubuntu 客户端是不是 `192.168.12.2`
- 两边是不是同一网段
- `robot-id` 是否正确

推荐先在 Ubuntu 上检查：

- `ip addr`
- `ping 192.168.12.1`

### 4.8 Ubuntu 客户端有 MQTT，但没有官方图传画面

如果没有：

- `[信息] 已收到官方图传 UDP 包...`

说明 `UDP 3334` 根本没收到数据。

优先检查：

- 官方选手端本身是否已经正常显示图传画面
- 是否真的在监听 `3334`
- 防火墙是否拦截 `3334/udp`

### 4.9 Ubuntu 客户端能收到 `CustomByteBlock`，但右侧英雄车画面不显示

先看是否出现：

- `[信息] 已收到自研视频配置包...`
- `[信息] 已收到新的关键帧，自研视频开始恢复解码`

如果只收到 `CustomByteBlock`，但没有配置包或关键帧日志，说明：

- `MCU` 虽然在发，但发的不是正确的自研视频完整链路
- 小电脑端配置包或关键帧没有正常到达

### 4.10 Ubuntu 客户端出现“自研视频包序号跳变”

如果看到：

- `[错误] 自研视频包序号跳变...`

说明链路中间有丢包或顺序错乱。

重点检查：

- `MCU` 队列是否满
- `0x0310` 是否有丢包
- 小电脑端码率是否过高

建议先降：

- `target-bytes`
- 输出分辨率
- 帧率

### 4.11 解码器报错

如果看到：

- `[错误] 解码器解析码流失败...`
- `[错误] 解码器送入码流失败...`
- `[错误] 解码器取出图像帧失败...`

说明问题通常在：

- 码流不完整
- 关键帧缺失
- 序号跳变后仍然继续喂了无效分片

优先看前面是否伴随：

- `序号跳变`
- `等待关键帧`
- `重置包`

## 5. 最省事的联调顺序

推荐严格按下面顺序，不要三端一起乱试：

### 第一步：只看官方图传

目标：确认 Ubuntu 客户端左侧画面正常。

你应该看到：

- MQTT 连接成功
- `UDP 3334` 收到包
- 官方 `HEVC` 成功解码

### 第二步：只打通小电脑到 `MCU`

目标：确认 `MCU` 至少能解析第一包并送出第一包 `0x0310`。

你应该看到：

- 小电脑端读到第一帧
- 小电脑端编码出第一段码流
- `MCU` 解析到第一包
- `MCU` 发出第一包 `0x0310`

### 第三步：打通自研视频到 Ubuntu

目标：确认右侧英雄车画面出现。

你应该看到：

- 客户端收到 `CustomByteBlock`
- 自研视频配置包到达
- 关键帧到达
- 解码器成功解出帧

## 6. 最关键的一条经验

如果你不知道问题在哪，不要先看“画面有没有出来”，而是按下面顺序看日志：

1. 小电脑有没有读到第一帧
2. 小电脑有没有编码出第一段码流
3. `MCU` 有没有解析到第一包
4. `MCU` 有没有发出第一包 `0x0310`
5. Ubuntu 有没有收到 `CustomByteBlock`
6. Ubuntu 有没有收到配置包和关键帧
7. 解码器有没有成功解出第一帧

只要按这个顺序查，基本就能定位是卡在第几步。
