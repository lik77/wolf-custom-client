# DJI RoboMaster 官方图传 H.265 码流接收与解码技术参考


---

## 一、网络层

| 项目 | 值 |
|------|-----|
| 传输协议 | UDP 单播/广播 |
| 端口 | **3334** |
| 绑定方式 | `0.0.0.0:3334`，`SO_SHAREADDRESS \| SO_REUSEADDR` |

图传数据不走 MQTT/Protobuf，是 DJI 官方图传模块直接在链路上发的裸 UDP 流。

---

## 二、包格式（核心）

每个 UDP 数据报的头部固定 **8 字节，大端序**：

```
Offset  Size  Type      Field
------  ----  --------  -------------------------------
 0       2B   uint16    帧编号 (frame_id)，递增，uint16 自然溢出绕回
 2       2B   uint16    当前帧内分片序号 (slice_idx)
 4       4B   uint32    当前帧总字节数 (total_size)
 8+     变长  二进制    码流数据体 (payload)
```

### 拆包代码 (Qt 风格, C++)

```cpp
uint16_t frame_id   = qFromBigEndian<uint16_t>(data + 0);
uint16_t slice_idx  = qFromBigEndian<uint16_t>(data + 2);
uint32_t total_size = qFromBigEndian<uint32_t>(data + 4);
QByteArray payload  = datagram.mid(8);
```

### Python 参考

```python
import struct
frame_id, slice_idx, total_size = struct.unpack('>HHI', data[:8])
payload = data[8:]
```

> **大端序是最容易踩的坑。** 如果用了小端序解头，frame_id 和 slice_idx 会完全错乱，帧永远拼不起来，解码器收不到完整的 NAL 单元也就吐不出任何画面。

---

## 三、帧重组逻辑

DJI 官方图传把每帧 H.265 码流切成多个 UDP 包发送，接收端需要按以下规则拼回完整帧：

1. 以 `frame_id` 为 key，在缓冲区中收集该帧的所有分片（`slice_idx -> payload`）。
2. 每收到一个分片，累加当前帧已收集的总字节数：
   ```cpp
   uint32_t accumulated = 0;
   for (auto &slice : buffer[frame_id])
       accumulated += slice.size();
   ```
3. **当 `accumulated >= total_size` 时，认定该帧接收完整。**
4. 按 `slice_idx` 升序拼接所有 payload，得到完整 H.265 码流。
5. 在缓冲区末尾**追加 `AV_INPUT_BUFFER_PADDING_SIZE`（即 64 字节）的零填充**（FFmpeg 内部解码需要，不加可能越界崩溃）。
6. 送入 FFmpeg 解码。
7. 解码完成后从缓冲区移除该帧。

### 缓冲清理策略

- 缓冲区 frame 数 > 5 时：扫描所有 key，`frame_id - key > 5`（差值在正常范围内，排除 uint16 绕回误判）的视为因丢包无法补齐的残缺帧，丢弃。
- 缓冲区 frame 数 > 20 时：强制淘汰最老帧，防止 OOM。

---

## 四、FFmpeg 解码器初始化

### 参数配置

```cpp
const AVCodec *codec = avcodec_find_decoder(AV_CODEC_ID_HEVC);  // H.265/HEVC

AVCodecContext *ctx = avcodec_alloc_context3(codec);
ctx->thread_count = 4;
ctx->flags2 |= AV_CODEC_FLAG2_CHUNKS;  // 关键！允许不完整帧输入

avcodec_open2(ctx, codec, nullptr);

AVCodecParserContext *parser = av_parser_init(codec->id);
```

### 关键点说明

| 参数 | 说明 |
|------|------|
| `AV_CODEC_ID_HEVC` | 硬编码为 H.265，不是 H.264 |
| `AV_CODEC_FLAG2_CHUNKS` | **必开。** 网络分片重组的帧可能在任意 NAL 边界被切断，不开此标志 FFmpeg 会拒绝解码"不完整"的帧。这是第二个常见坑。 |
| `thread_count = 4` | 解码线程数，可按 CPU 核数调整 |
| `AVCodecParserContext` | 用于从字节流中切分 NAL 单元。H.265 的起始码是 `0x00 0x00 0x00 0x01`，parser 会自动识别，不需要手动扫描。 |

---

## 五、解码循环

```cpp
void decodeBuffer(uint8_t *data, int size) {
    while (size > 0) {
        // 1. 用 parser 从字节流中切出一个完整 NAL 单元
        int len = av_parser_parse2(parser, ctx,
                                   &pkt->data, &pkt->size,
                                   data, size,
                                   AV_NOPTS_VALUE, AV_NOPTS_VALUE, 0);
        data += len;
        size -= len;

        if (pkt->size > 0) {
            // 2. 送入解码器
            avcodec_send_packet(ctx, pkt);

            // 3. 循环取解码后的帧（一包可能解出多帧，也可能零帧）
            while (true) {
                int ret = avcodec_receive_frame(ctx, frame);
                if (ret == AVERROR(EAGAIN) || ret == AVERROR_EOF)
                    break;          // 正常：需要更多数据 或 流结束

                // 4. 深度拷贝！（见下文）
                AVFrame *clone = av_frame_clone(frame);
                // ... 送去渲染
            }
            av_packet_unref(pkt);
        }
    }
}
```

### av_frame_clone 深度拷贝

**这是第三个常见坑。** `avcodec_receive_frame` 返回的 `AVFrame` 内部 buffer 归 FFmpeg 管理，下一次 `avcodec_send_packet` 或 `avcodec_receive_frame` 就可能释放/覆盖。如果直接把裸指针发到渲染线程，会出现**花屏、闪屏或崩溃**。

正确做法：`av_frame_clone()` 创建完全独立的拷贝，再跨线程传递。

---

## 六、解码输出格式

| 属性 | 值 |
|------|-----|
| 像素格式 | `AV_PIX_FMT_YUV420P`（或 `AV_PIX_FMT_YUVJ420P`） |
| 分辨率 | 由码流 SPS 决定，解码后从 `frame->width / frame->height` 取 |
| Y 平面 | `width × height` 字节，linesize 可能 > width |
| U/V 平面 | `(width/2) × (height/2)` 字节，linesize 可能 > width/2 |

> **注意 linesize 对齐。** FFmpeg 为 SIMD 优化可能让 linesize 大于实际宽，逐行 `memcpy` 时要用 `frame->linesize[i]` 而不是 `width`。否则画面会出现阶梯状错位。

---

## 七、YUV → RGB 转换参数

OpenGL 片段着色器中使用的转换公式（BT.601 标准）：

```glsl
float y = texture2D(tY, tex).r;
float u = texture2D(tU, tex).r - 0.5;
float v = texture2D(tV, tex).r - 0.5;

R = y + 1.403 * v
G = y - 0.344 * u - 0.714 * v
B = y + 1.770 * u
```

GPU 纹理格式：`GL_LUMINANCE`，三个平面各一个独立纹理。

---

## 八、帧尺寸硬限制

```cpp
if (total_size > 2000000 || total_size == 0)
    // 丢弃
```

单帧超过 **2 MB** 会被直接丢弃。对于 1080p H.265，正常的 P 帧通常远小于此，但 **IDR 关键帧**可能接近甚至超过 2 MB，偶尔会导致关键帧被丢弃、画面短暂卡住等待下一个 IDR。如果网络状况较好的场景下画面仍间歇性黑屏，可以把这个阈值调大（如 4 MB）。

---

## 九、常见坑点速查

| # | 现象 | 大概率原因 |
|---|------|-----------|
| 1 | 能收到包但完全没画面 | **大端序/小端序搞反了。** 头 8 字节必须用大端序解析。 |
| 2 | 偶尔花屏或程序崩溃 | **没用 `av_frame_clone` 深拷贝**，FFmpeg 内部释放了还在用的 buffer。 |
| 3 | 解码器初始化成功但始终不出帧 | **没开 `AV_CODEC_FLAG2_CHUNKS`**，网络分片重组后的帧在 NAL 边界不完整，FFmpeg 拒绝处理。 |
| 4 | 画面阶梯状错位/偏色 | **忽略 linesize**，直接按 width 连续拷贝了带有 padding 的行数据。 |
| 5 | 画面周期性短暂黑屏 | **2 MB 阈值**挡住了 IDR 帧，关键帧被丢弃导致等下一个 IDR 才有画面。 |
| 6 | 忘记加 `AV_INPUT_BUFFER_PADDING_SIZE` 零填充 | FFmpeg 内部 H.265 解码器在 buffer 末尾会多读几个字节做预测，不加可能随机崩溃或花屏。 |
| 7 | `accumulated >= total_size` 误判 | 如果某帧丢了一个分片，不会触发完成条件，会被后续缓存清理机制淘汰（frame_id 落后 > 5 帧后丢弃）。 |

---

## 十、接收端最小实现参考（伪代码）

```
socket = bind_udp(0.0.0.0, 3334, SO_SHAREADDR | SO_REUSEADDR)
decoder = ffmpeg_hevc_decoder(threads=4, chunks=true)
buffer  = map<frame_id, map<slice_idx, bytes>>

loop:
    pkt = socket.recv()
    frame_id, slice_idx, total_size = be_unpack(>HHI, pkt[0:8])
    payload = pkt[8:]

    if total_size > MAX_FRAME or total_size == 0: continue

    buffer[frame_id][slice_idx] = payload
    accumulated = sum(len(s) for s in buffer[frame_id])

    if accumulated >= total_size:
        full = concat(buffer[frame_id] sorted by slice_idx)
        full += zeros(AV_INPUT_BUFFER_PADDING_SIZE)
        buffer.remove(frame_id)

        while full has data:
            nal = parser.parse(full)
            if nal:
                decoder.send(nal)
                while frame = decoder.receive():
                    clone = deep_clone(frame)
                    render(clone)
```

---

以上是 265 官方图传从 UDP 收到 OpenGL 渲染的完整链路。如果哥那边有具体的报错或现象可以对一下上面的坑点表，大部分"收到包但解不了码"的情况都是第 1、2、3 条。
