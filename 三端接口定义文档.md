# 三端接口定义文档

## 1. 目标

本文把整套方案拆成 3 份代码和 1 份共享协议定义：

- `hero-camera-agent`：英雄车上小电脑端，负责相机采集、画面预处理、编码、切片发送。
- `rm-video-bridge`：电控端桥接模块，负责接收小电脑视频切片，并映射到 RoboMaster 官方上行链路。
- `rm-custom-client`：Ubuntu 自定义客户端，负责接收比赛数据、接收两路视频、解码显示、和自定义控制器通信。
- `rm-shared-protocol`：三端共享的常量、结构体、`.proto` 和测试数据。

本文同时区分两类接口：

- 官方固定接口：必须按赛事协议实现，不能随意改。
- 自研接口：小电脑端到电控端、以及 0x0310 载荷内部格式，可由队伍自行定义。

## 2. 总体链路

建议按下面的逻辑理解整个系统：

1. 英雄车小电脑连接工业相机，采集实时画面。
2. 小电脑对画面做低带宽优化后编码，生成连续视频字节流。
3. 小电脑把字节流切成适合 `0x0310` 上行链路的小包，发给电控端。
4. 电控端把这些小包转发到机器人图传上行自定义数据链路 `0x0310`。
5. 官方选手端通过图传链路接收机器人上行数据，再通过 `MQTT + Protobuf` 把 `CustomByteBlock` 转给 Ubuntu 自定义客户端。
6. Ubuntu 自定义客户端同时监听 `UDP 3334`，接收官方图传 `HEVC` 码流。
7. Ubuntu 自定义客户端最终同时显示：
8. 一路是官方图传画面；
9. 一路是英雄车自研视频链路画面。

## 3. 官方固定接口

### 3.1 自定义客户端接入方式

根据官方配置说明，当前推荐网络拓扑不是 Ubuntu 直接代替官方选手端，而是：

- 一台运行官方选手端的主机连接赛事引擎局域网。
- 同一台主机再通过第二张有线网卡连接 Ubuntu 自定义客户端。
- 官方选手端主机连接自定义客户端的网卡固定为 `192.168.12.1/24`。
- Ubuntu 自定义客户端固定为 `192.168.12.2/24`。

这意味着：

- 如果你们后面想让 Ubuntu 机器直接顶替官方选手端主机，需要先确认官方选手端是否支持该部署方式。
- 在未确认前，开发默认按“官方选手端主机中转 + Ubuntu 自定义客户端”实现。

### 3.2 比赛数据与控制数据

- 传输协议：`MQTT + Protobuf v3`
- Protobuf 版本：`3.15` 及以上
- 服务器地址：`192.168.12.1:3333`
- 客户端地址：`192.168.12.2`
- Topic：官方文档中定义的指令名，例如 `CustomByteBlock`、`GameStatus`、`RobotDynamicStatus`
- `clientID`：填写所连接机器人对应的 ID，例如红方英雄为 `1`，蓝方英雄为 `11`

对于你当前需求，Ubuntu 客户端至少要处理这些 Topic：

- `CustomByteBlock`：机器人通过 `0x0310` 送来的自定义数据流
- `GameStatus`
- `RobotDynamicStatus`
- `RobotModuleStatus`
- 其他比赛态势 Topic 可后续按需补齐

### 3.3 官方图传画面

- Ubuntu 自定义客户端通过 `UDP` 监听 `3334` 端口获取图传码流。
- 编码格式固定为 `HEVC`。
- 每个 UDP 包前 8 字节为固定头部：
- `frame_id`：2 字节
- `slice_index`：2 字节
- `frame_total_bytes`：4 字节

实现注意事项：

- 文档提到了字段长度，但没有在提取文本中明确字节序，建议先抓包确认，默认按小端解析并保留切换开关。
- 只有在官方选手端已经正常显示图传画面时，官方选手端才会向外发送 `UDP 3334` 视频流。

### 3.4 机器人到自定义客户端的自定义上行数据

- 官方命令字：`0x0310`
- 单次有效载荷：`300 Byte`
- 方向：`机器人 -> 图传链路 -> 自定义客户端`
- 自定义客户端侧对应 MQTT Topic：`CustomByteBlock`
- 协议定义：`message CustomByteBlock { optional bytes data = 1; }`
- 频率上限：`50Hz`
- 特性：无重传机制

这条链路是你们承载“英雄车自研相机画面”的核心链路。

### 3.5 自定义客户端到机器人

如需后续加入控制回传，还会用到：

- `KeyboardMouseControl`：键鼠控制，最终走 `0x0311`
- `CustomControl`：最多 30 字节自定义数据，最终走 `0x0311`

当前版本如果只做收流和显示，可以先不实现这部分。

## 4. 自研链路设计原则

### 4.1 为什么不能直接传原始图像

`0x0310` 的理论上限约为：

- `300 Byte * 50Hz = 15000 Byte/s`

结合官方开源文档中的建议，实际视频流应按 `12kB/s` 左右设计，才能给丢包和抖动留余量。

因此：

- 不能传原始帧。
- 必须先做 ROI、降采样、静态信息剔除。
- 必须使用视频编码。
- 必须把输出控制成连续低码率字节流。

### 4.2 V1 编码建议

自研英雄车画面链路的 V1 版本建议如下：

- 采集帧率：`60fps`
- 输出显示目标：`20fps` 到 `50fps`，由码率和丢包情况决定
- 分辨率：`300x300` 起步，协议带宽提高后可尝试 `400x400`
- 预处理：`ROI + 降采样 + 静态背景剔除 + 可选弹道拖影`
- 编码格式：V1 推荐 `H.264 Annex-B`
- 目标输出码率：`10kB/s` 到 `12kB/s`
- 关键帧策略：定期插入关键帧，便于丢包后快速恢复

说明：

- 官方 `UDP 3334` 固定是 `HEVC`，这不代表你们自研英雄车链路也必须用 `HEVC`。
- 五大湖开源中明确提到他们出于实现稳定性优先使用 `H.264`，`H.265/HEVC` 低码率效果更好，但编码开销更大。
- 因此 V1 建议先用 `H.264` 跑通整链路，后续再评估升级 `HEVC`。

## 5. 自研共享消息格式

### 5.1 设计目标

自研消息格式要满足以下约束：

- 能同时跑在“小电脑 -> 电控端”链路和 `0x0310` 载荷内部。
- 单个消息在 `0x0310` 中总长度不能超过 `300 Byte`。
- 电控端尽量不做复杂视频解析，只做校验、排队、转发、限速。
- Ubuntu 客户端能够根据序号判断丢包，并在丢包后等待下一关键帧恢复。

### 5.2 V1 包头定义

建议三端统一使用下面的二进制包头，采用小端：

```c
typedef struct __attribute__((packed)) {
    uint16_t magic;       // 固定 0x4856, 即 'HV'
    uint8_t  version;     // 固定 1
    uint8_t  packet_type; // 1=config, 2=stream_chunk, 3=heartbeat, 4=reset
    uint8_t  stream_id;   // 固定 1, 预留未来多路相机
    uint8_t  codec;       // 1=h264_annexb, 2=hevc_annexb, 3=mjpeg
    uint8_t  flags;       // bit0=keyframe_hint, bit1=config_repeat, bit2=discontinuity
    uint8_t  reserved0;
    uint32_t seq;         // 每发一个包递增 1
    uint16_t payload_len; // 当前包负载长度
    uint16_t packet_crc16; // 对包头前 14 字节和 payload 做 CRC16/CCITT-FALSE
} hero_video_packet_header_v1_t;
```

约束：

- 头长固定 `16 Byte`
- 当承载到 `0x0310` 时，`payload_len <= 284`
- `seq` 只对“包”编号，不对“帧”编号
- 视频数据以“连续编码字节流”方式发送，而不是“每帧一个协议包”
- `packet_crc16` 不参与自身计算，接收端校验失败直接丢包

### 5.3 packet_type 定义

- `1 = config`
- `2 = stream_chunk`
- `3 = heartbeat`
- `4 = reset`

其中最关键的是 `stream_chunk`。

### 5.4 config 载荷定义

`packet_type = 1` 时，`payload` 建议如下：

```c
typedef struct __attribute__((packed)) {
    uint16_t width;
    uint16_t height;
    uint16_t fps;
    uint16_t target_bytes_per_sec;
} hero_video_config_v1_t;
```

建议：

- 小电脑端启动时先发 1 个 `config`
- 之后每隔 `1s` 重发一次 `config`
- 切换分辨率、编码器或码率时立即重发 `config`

### 5.5 stream_chunk 载荷定义

`packet_type = 2` 时，`payload` 直接放编码器输出的连续字节流切片：

- 若使用 `H.264`，则 payload 为 `Annex-B` 字节流切片
- 若使用 `HEVC`，则 payload 为 `HEVC Annex-B` 字节流切片
- 小电脑端负责把字节流切成 `<= 284 Byte` 的片段
- 每个片段对应一个 `hero_video_packet_header_v1_t + payload`

这样设计的好处是：

- 小电脑端不必等待整帧编码完成后再发“大包”
- 电控端只需要转发小包，不必理解视频帧边界
- Ubuntu 客户端只要按 `seq` 拼接连续字节流，再喂给解码器即可

### 5.6 heartbeat 和 reset

- `heartbeat`：建议 `1Hz` 发送，payload 可留空，或带上队列长度、丢帧数、编码延迟等状态
- `reset`：编码器重启、分辨率切换或检测到严重丢包时发送，Ubuntu 客户端收到后立即清空缓存并等待新的 `config` 和关键帧

## 6. 三端接口定义

### 6.1 小电脑端 `hero-camera-agent`

职责：

- 接相机
- 采集图像
- 做低带宽预处理
- 编码成连续字节流
- 按 `hero_video_packet_header_v1_t` 切片
- 发送给电控端

推荐输入：

- 工业相机原始画面

推荐输出：

- `config`
- `stream_chunk`
- `heartbeat`
- `reset`

物理传输建议：

- 电控端已经确认是 `MCU`，V1 建议优先使用独立 `UART`，不要先上 `UDP`
- 推荐串口参数：`8N1`、全双工、`921600` 起步，优先尝试 `1500000` 或 `2000000`
- 推荐发送方式：用户态发送线程 + 串口驱动连续写，不等待逐包应答
- 若现有硬件具备稳定的 `USB CDC`，可作为 V2 备选，但不建议作为首版联调方案
- 无论物理层如何变化，消息体保持完全一致

如果你现在就要定一个 V1 方案，建议直接定为：

- 小电脑端到电控端：独立 `UART`
- 波特率：`1500000`
- 包格式：`hero_video_packet_header_v1_t + payload`
- 小电脑端只管连续发送，MCU 侧负责收包校验和排队

如果后续硬件验证 `1500000` 不稳定，可降到 `921600` 并同步收紧编码码率。

### 6.2 电控端 `rm-video-bridge`

职责：

- 接收小电脑端的 `hero_video_packet_header_v1_t`
- 校验 `magic/version/payload_len/packet_crc16`
- 做简单排队
- 按 `50Hz` 节奏发送到官方 `0x0310`
- 不做复杂视频解码和重编码

输入：

- 小电脑端发来的 `config/stream_chunk/heartbeat/reset`

输出：

- 官方 `0x0310`

MCU 版本实现建议：

- 串口接收使用 `DMA + 环形缓冲区`
- 在任务上下文中做包头扫描和 CRC 校验，不要在中断里做复杂解析
- 转发队列使用固定长度环形队列，避免动态内存分配
- `config`、`reset`、`heartbeat` 与 `stream_chunk` 分优先级处理
- 若 MCU 上已有 `FreeRTOS`，建议拆成“串口接收任务”和“0x0310 发送任务”
- 若是裸机工程，建议采用“串口 DMA 接收 + 主循环状态机 + 定时器节拍发送”

转发规则：

1. 每个官方 `0x0310` 只装 1 个完整自研小包
2. 若自研小包总长度大于 `300 Byte`，直接丢弃并记日志
3. 若发送队列堆积，优先丢弃旧的 `stream_chunk`
4. `config` 和 `reset` 不要轻易丢
5. 电控端不保证重传
6. 电控端不保证严格恢复视频帧边界
7. 串口解析发现 CRC 错误或长度异常时，直接丢包并重新扫描下一个 `magic`

### 6.3 Ubuntu 自定义客户端 `rm-custom-client`

职责：

- 连接 `192.168.12.1:3333`
- 订阅比赛状态相关 Topic
- 订阅 `CustomByteBlock`
- 从 `CustomByteBlock.data` 中解析自研英雄车视频小包
- 根据 `seq` 检测丢包
- 把连续视频字节流送给解码器
- 同时监听 `UDP 3334`
- 同时显示官方图传和英雄车画面

输入：

- MQTT Topic：`CustomByteBlock`
- MQTT Topic：比赛状态 Topic
- UDP：`3334`

输出：

- 两路实时视频画面
- 状态日志
- 可选控制回传

客户端处理规则：

1. 启动后先连接 MQTT，再监听 `UDP 3334`
2. 收到 `config` 后建立或更新英雄车画面的解码上下文
3. 收到 `stream_chunk` 后按 `seq` 连续性检查
4. 若 `seq` 不连续，标记丢包并暂停继续送码，直到遇到 `reset` 或下一次关键帧恢复点
5. 官方 `UDP 3334` 链路单独处理，不能和自研英雄车视频共用重组逻辑
6. 两路视频分别维护各自的缓冲区、解码器和统计信息

## 7. 推荐工程边界

### 7.1 车上小电脑端仓库

- 不包含任何 RoboMaster 官方 MQTT 逻辑
- 不关心 `192.168.12.1:3333`
- 只负责“把相机画面变成低码率小包”

### 7.2 电控端仓库

- 不负责视频编码
- 不负责 GUI
- 不负责官方 `UDP 3334`
- 只负责桥接和节流
- 默认按 `MCU` 工程设计，尽量不用动态分配

### 7.3 Ubuntu 客户端仓库

- 不负责相机采集
- 不负责机器人内部传输
- 只负责赛事接入、两路收流、解码显示、控制接口

## 8. 开发顺序建议

建议按下面的顺序推进，最省时间：

1. 先做 `rm-custom-client`，把官方 `UDP 3334` 图传画面单独显示出来
2. 再做 `rm-custom-client` 的 `MQTT + Protobuf + CustomByteBlock` 接收框架
3. 再做 `hero-camera-agent` 的视频采集和低码率编码
4. 再做 `rm-video-bridge`，把自研小包打进 `0x0310`
5. 最后把英雄车画面在 Ubuntu 客户端第二窗口显示出来

原因是：

- 官方 `UDP 3334` 画面最容易先验证显示链路
- `CustomByteBlock` 跑通后，自研视频只是在上面再承载一层字节流
- 这样排查问题时能快速区分是“官方接入问题”还是“自研视频链路问题”

## 9. 当前必须确认的事项

开始写代码前，建议先明确下面 3 件事：

1. Ubuntu 自定义客户端是否一定要独立于官方选手端主机，如果是，则默认按 `192.168.12.1 <-> 192.168.12.2` 方式接入
2. 英雄车自研画面 V1 是否接受 `H.264`，如果接受，V1 落地会明显更快
3. 官方 `UDP 3334` 头部字节序是否为小端，建议抓包确认一次

## 10. 文档依据

本文中的官方固定接口主要依据以下提取文本整理：

- `/tmp/opencode/client_config.txt`
- `/tmp/opencode/protocol.txt`
- `/tmp/opencode/video_link.txt`

建议后续开发时，始终把“官方固定接口”和“自研接口”分开维护，避免后面误把官方链路字段改掉。
